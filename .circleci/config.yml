version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:current
    steps:
      # making a contribution as a user outside of kelvintayw-cci GH org
      - run: printenv FOOBAR > foobar.txt
      - store_artifacts:
          path: foobar.txt

workflows:
  main:
    jobs:
      - build:
          context:
            # account context on my Server 4.x
            - dummy
